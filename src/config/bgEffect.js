// 全局背景动效配置
// 可选类型：'waves'（流体）、'particles'（粒子）等
const bgEffectConfig = {
  type: 'waves', // 可选 'waves' | 'particles'
};

export default bgEffectConfig;

// 新建 interview.js 配置文件
export const nextButtonText = '继续';
export const nextButtonSendText = '继续'; 